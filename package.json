{
  "name": "rails-starterpack",
  "type": "module",
  "devDependencies": {
    "husky": "^9.0.7",
    "lint-staged": "^15.2.0"
  },
  "peerDependencies": {},
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "*.rb": "docker compose run --rm --no-deps web standardrb"
  }
}
